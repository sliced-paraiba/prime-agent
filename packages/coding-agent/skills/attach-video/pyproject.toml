[project]
name = "prime-agent-skill-attach-video"
version = "0.1.0"
description = "Load on-disk video files into the model's context as viewable attachments."
requires-python = ">=3.10"
dependencies = ["prime-agent-runtime"]

[project.scripts]
attach_video = "rlm.skill:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/attach_video"]
